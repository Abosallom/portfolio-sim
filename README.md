# Virtual $1M US Portfolio Simulator

Live simulation dashboard: https://abosallom.github.io/portfolio-sim/

Real quotes via finnhub.io (free API key). All state stored in the browser. Simulation only — no real money.
